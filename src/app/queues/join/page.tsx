export default function JoinQueuePage() {
  return (
    <main style={{ padding: 16 }}>
      <h1 style={{ fontSize: 22, fontWeight: 700, marginBottom: 12 }}>Join Queue</h1>
      <p style={{ opacity: .8, marginBottom: 16 }}>Enter code or pick a nearby queue.</p>
      {/* TODO: code input + nearby list */}
      <div style={{ padding: 12, borderRadius: 12, background: '#0b1220', color: 'white' }}>
        Stub page — code input + list here.
      </div>
    </main>
  );
}
